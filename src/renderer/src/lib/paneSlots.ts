// Pane slots: types sharing a slot replace each other in the layout tree
// instead of opening a second window. The left sidebar family shares one slot,
// the editor family another.

export const SIDEBAR_SLOT = 'sidebar'
export const CENTER_SLOT = 'center'
